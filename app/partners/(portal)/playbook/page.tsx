import ComingSoon from "../ComingSoon";

export default function PlaybookPage() {
  return (
    <ComingSoon
      title="Playbook"
      intro="The bits that actually move the needle — what to post, what to say, and when to push. Copy it, change the name, use it."
      bullets={[
        "Social posts written for your gym, per platform, ready to copy",
        "Member emails — the announcement, the re-engagement, the 'your PT wants to qualify'",
        "In-gym scripts for the front desk, the floor and the tour close",
        "Campaign plays — January intake, QR poster push, member referral",
      ]}
    />
  );
}
